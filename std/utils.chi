package std/utils

import std/lang.option { Option }

pub fn range(from: int, to: int): () -> Option[int] {
    var next = from
    { 
        if next < to {
            (next += 1) - 1
        }
    }
}

